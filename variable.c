#include <stdio.h>
#include <stdlib.h>

int main(){
    int y = 71;
    printf("The value as an int is %d\n", y);
    printf("The value as a float is %f\n", y);
    printf("The value as a char is %c\n", y);
    return 0;
}
